general {
  colors = true
  interval = 1
  color_separator = "#B3BEFF"
}

order += "volume master"
order += "tztime local"

tztime local {
  format = "%Y-%m-%d %H:%M:%S"
}

volume master {
  format = "♪ %volume"
  format_muted = "M %volume"
  device = "default"
  mixer = "Master"
  mixer_idx = 0
}
